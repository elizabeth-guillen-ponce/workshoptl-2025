<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.Workshoptl" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Workshoptl" parent="Base.Theme.Workshoptl">
        <item name="colorPrimaryDark">@color/blue_navy</item>
        <item name="colorPrimary">@color/blue</item>
        <item name="colorAccent">@color/blue_navy</item>
    </style>


    <style name="Widget.App.Button" parent="Widget.MaterialComponents.Button">
        <item name="android:textColor">@color/white</item>  <!-- Define in colors.xml -->
        <item name="backgroundTint">@color/blue</item> <!-- Define in colors.xml -->
        <item name="cornerRadius">8dp</item>  <!-- Example: rounded corners -->
        <item name="android:paddingTop">12dp</item>
        <item name="android:paddingBottom">12dp</item>
        <item name="android:textSize">16sp</item>
        <!-- Add other attributes as needed, like font, elevation, etc. -->
    </style>
</resources>